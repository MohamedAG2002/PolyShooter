#pragma once

#include <string>

namespace ps { // beginning of ps

namespace consts { // beginning of consts
  
const int SCREEN_WIDTH = 384;
const int SCREEN_HEIGHT = 512;
const std::string FILE_NAME = "data.dat";

} // end of consts

} // end of ps
